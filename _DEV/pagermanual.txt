 <Box
            sx={{
              border: '1px solid #ddd',
              padding: '15px',
              margin: '20px 0',
              backgroundColor: '#f9f9f9',
              // Opsional: Tambahkan border radius agar lebih modern
              borderRadius: '4px',
            }}
          >
            <Typography variant="h6" component="h2" gutterBottom>
              STATUS ALAT SAAT INI
            </Typography>

            <Typography
              variant="body1"
              sx={{
                fontWeight: 'bold',
                color: currentStatus.motor_status === 'MOVING' ? 'orange' : 'green',
                mb: 1 // margin-bottom 1 (untuk memisahkan baris)
              }}
            >
              MOTOR: {currentStatus.motor_status}
            </Typography>
            <Typography
              variant="body1"
              sx={{
                fontWeight: 'bold',
                color: currentStatus.motor_status === 'MOVING' ? 'orange' : 'green',
                mb: 1 // margin-bottom 1 (untuk memisahkan baris)
              }}
            >
              STATUS: <strong>{status}</strong>
            </Typography>

            <Typography variant="body2" sx={{ mb: 0.5 }}>
              Posisi Kanan Terkunci: **{currentStatus.posisi_kanan ? 'YA' : 'TIDAK'}**
            </Typography>

            <Typography variant="body2">
              Posisi Kiri Terkunci: **{currentStatus.posisi_kiri ? 'YA' : 'TIDAK'}**
            </Typography>
          </Box>

          <Box
            sx={{
              margin: '30px 0',
              display: 'flex', // Memastikan tombol berada dalam satu baris
              gap: '20px' // ✅ Alternatif yang lebih modern untuk marginLeft
            }}
          >
            {/* Tombol KIRI */}
            <Button
              variant="contained" // Contoh: Gunakan variant contained
              onClick={() => handleMove('kiri')}
              disabled={loading}
              sx={{
                minWidth: 100, // Menjaga lebar minimum agar tombol seragam
                bgcolor: '#395886',
                '&:hover': {
                  bgcolor: '#2f4870',
                }
              }}
            >
              {loading ? <CircularProgress size={24} color="inherit" /> : '⬅️ KIRI'}
            </Button>

            {/* Tombol KANAN */}
            <Button
              variant="contained"
              onClick={() => handleMove('kanan')}
              disabled={loading}
              sx={{
                minWidth: 100,
                bgcolor: '#395886',
                '&:hover': {
                  bgcolor: '#2f4870',
                }
              }}
            >
              {loading ? <CircularProgress size={24} color="inherit" /> : 'KANAN ➡️'}
            </Button>
          </Box>